image:
- Visual Studio 2022
environment:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
build_script:
- appveyor DownloadFile https://dot.net/v1/dotnet-install.ps1
# global.json pins the .NET 11 SDK, which builds every target. The Tests project is net10.0 and is
# launched as a net10.0 app by `dotnet test`, so the .NET 10 runtime must be installed alongside it.
- pwsh: ./dotnet-install.ps1 -JSonFile global.json -Architecture x64 -InstallDir 'C:\Program Files\dotnet'
- pwsh: ./dotnet-install.ps1 -Channel 10.0 -Runtime dotnet -Architecture x64 -InstallDir 'C:\Program Files\dotnet'
- dotnet build src --configuration Release
- dotnet test src --configuration Release --no-build --results-directory TestResults -- --report-trx --report-trx-filename Tests.trx --coverage --coverage-output-format cobertura --coverage-output coverage.cobertura.xml
on_finish:
- ps: |
    Get-ChildItem TestResults\*.trx -ErrorAction SilentlyContinue | % {
      (New-Object 'System.Net.WebClient').UploadFile("https://ci.appveyor.com/api/testresults/mstest/$($env:APPVEYOR_JOB_ID)", $_.FullName)
    }
    Get-ChildItem TestResults\*.cobertura.xml -ErrorAction SilentlyContinue | % { Push-AppveyorArtifact $_.FullName -FileName $_.Name }
on_failure:
  - ps: |
      $root = (Get-Location).Path
      Get-ChildItem *.received.* -Recurse | % {
        $rel = $_.FullName.Substring($root.Length + 1)
        Push-AppveyorArtifact $_.FullName -FileName $rel
      }
test: off
artifacts:
- path: nugets\*.nupkg
- path: TestResults\*.cobertura.xml
